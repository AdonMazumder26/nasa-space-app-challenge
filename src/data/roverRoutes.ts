import type { RoverRoute } from "../types/catalog";

/**
 * Surveyed rover traverses are not part of this catalog.
 * The trail renderer draws a route only when one is listed here.
 */
export const roverRoutes: RoverRoute[] = [];
